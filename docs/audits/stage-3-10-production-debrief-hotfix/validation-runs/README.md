# validation-runs/

Local command logs for the pre-deployment gate. Summaries are recorded in
`../test-results.md` and `../pre-deployment-gate.md`.

These `.log` files are kept untracked to avoid committing large PowerShell-tee
artifacts; regenerate by re-running the gate commands if needed.
